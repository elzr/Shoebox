@import '_mixins.sass';

body {margin:0; padding:0; font-family:Verdana, sans-serif; overflow:hidden}
#shoebox {background-color:#a0754b; text-align:center; position:relative; padding-top:800px;
	img.background {position:absolute; z-index:1; top:0; left:0; width:100%; height:800px}
	.loading {text-align:center; position:absolute; top:250px; left:45%; z-index:2; margin:20px; padding:20px; background:#eee; font-style:italic; @include corners(); }
	.overlay {position:absolute; top:0; left:0; z-index:3; width:100%; height:800px}
	#canvas {position:absolute; width:100%; height:800px; z-index:4; top:0; left:0;
		div.pic { position:absolute; display:inline-block; color:#000; text-decoration:none; overflow:hidden; background:#555; @include shadows(#888, 2px, 2px, 4px); cursor:pointer;
			img {position:absolute; border:5px solid #fff; top:0; left:0}
			.backside {position:absolute; visibility:hidden; text-align:center; background:#999; color:#fff;
				span {display:block; font-family:Times New Roman, serif; padding:20px 10px; background:#555; font-weight:bold}
			}
		}
		div.flipped { z-index:2; @include shadows(#888, 2px, 2px, 4px);
			img {visibility:hidden}
			.backside {visibility:visible}
		}
		div.ui-draggable-dragging {z-index:3; @include shadows(#555, 8px, 8px, 16px)}

		.logo-mom {position:absolute; bottom:90px; left:20px; z-index:1}
		#trash {position:absolute; bottom:0; left:0; width:100%; height:75px; z-index:1000; cursor:pointer; 
			.text {position:absolute; z-index:2; top:0; left:0; margin-top:20px; color:#fff; font-size:90%; width:100%; height:50px; font-weight:bold}
				span.full {display:none; color:#ddd; font-size:90%; font-weight:normal}
			.background {background:#856d59; @include opacity(50); position:absolute; top:0; left:0; width:100%; height:75px; z-index:1}
		}
			#trash.full {@include opacity(70);
				span.full {display:inline-block}
			}
			#trash.hover {
				.background {background:#fff; @include opacity(100)}
				span {color:#000}
				span.full {display:none}
			}
	}
	#toolbar {position:absolute; top:0; left:0; height:100px; text-align:center; width:100%; z-index:5;
		.container {padding:10px}
		.title {display:inline-block; width:278px; height:100px; background:url(/img/label-maker.jpg);
			input {border:0; background:transparent; width:278px; height:100px; font-size:20px; text-align:center; font-weight:bold}
		}
		.logo-1000 img {vertical-align:middle; margin-right:30px}
	}
}
.ico {width:32px; height:32px; vertical-align:middle}
#toolbar input {font-family:"Monotype Corsiva", "Apple Chancery", cursive; }
