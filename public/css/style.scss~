@import '_mixins.sass';

body {margin:0; padding:0; font-family:Verdana, sans-serif}
#shoebox {border:30px inset #ccc; background:#eee; text-align:center; 
	#canvas {padding:20px; height:800px; position:relative;
		a { position:absolute; display:inline-block; color:#000; text-decoration:none; overflow:hidden; background:#555; @include shadows(#888, 2px, 2px, 4px);
			img {position:absolute; border:5px solid #fff; top:0; left:0}
			span {position:absolute; visibility:hidden; display:inline-block; width:100%; text-align:center}
		}
		a.backside { z-index:2; @include shadows(#888, 2px, 2px, 4px);
			img {visibility:hidden}
			span {visibility:visible; padding:10px 5px; font-weight:bold; background:#555; color:#fff}
		}
		a.ui-draggable-dragging {z-index:3; @include shadows(#555, 8px, 8px, 16px)}
	}
}
.ico {width:32px; height:32px; vertical-align:middle}
