@import '_mixins.sass';

body {margin:0; padding:0; font-family:Verdana, sans-serif; overflow:hidden}
#shoebox {background-color:#a0754b; text-align:center; position:relative; padding-top:800px;
	#toolbar {position:absolute; top:0; left:0; height:100px; text-align:center; width:100%; z-index:5;
		.container {padding:10px}
		.title {display:inline-block; vertical-align:middle; width:250px; height:100px; text-align:center;
			.tl, .tr, .br, .bl {width:30px; height:30px}
				.tl {background:url(/img/title-tl.jpg)}
				.tr {background:url(/img/title-tr.jpg)}
				.br {background:url(/img/title-br.jpg)}
				.bl {background:url(/img/title-bl.jpg)}
			.t, .b {height:30px; width:190px}
				.t {background:url(/img/title-t.jpg)}
				.b {background:url(/img/title-b.jpg)}
			.l, .r {width:30px; height:40px}
				.l {background:url(/img/title-l.jpg)}
				.r {background:url(/img/title-r.jpg)}
			.c {background:url(/img/title-c.jpg)}
		}
		textarea, .shadow {border:0; background:transparent; line-height:40px; font-size:25px; text-align:center; font-weight:bold}
			textarea {width:190px; height:40px; overflow:hidden; outline:none}
			.shadow {display:none; max-width:500px; max-height:200px; white-space:pre-wrap}
		.logo-1000 img {vertical-align:middle; margin-right:30px}
		#sorts { color:#fff; display:inline-block; font-size:13px; margin-left:30px; font-weight:bold;
			a { color:#000; text-decoration:none; display:inline-block; padding:7px 12px; margin-left:5px; @include corners(7px); @include vertical-gradient(#d9cbb8, #d5bca1); @include shadows(#786a5b, 2px, 2px, 4px); background-color:#D2E0EE; font-weight:normal}
				a:hover, a.selected {color:#000; @include vertical-gradient(#ede1d1, #bba68c); background-color:#D2E0EE}
				a.selected {@include inset-shadows(#786a5b, 2px, 2px, 4px); text-decoration:underline}
		}
	}
	img.background {position:absolute; z-index:1; top:0; left:0; width:100%; height:800px}
	.loading {text-align:center; position:absolute; top:250px; left:45%; z-index:2; margin:20px; padding:20px; background:#eee; font-style:italic; }
	.overlay {position:absolute; top:0; left:0; z-index:3; width:100%; height:800px}
	#canvas {position:absolute; width:100%; height:800px; z-index:4; top:0; left:0;
		div.pic { position:absolute; display:inline-block; color:#000; text-decoration:none; overflow:hidden; background:#555; @include shadows(#888, 2px, 2px, 4px); cursor:pointer;
			img {position:absolute; border:5px solid #fff; top:0; left:0}
			.backside {position:absolute; visibility:hidden; text-align:center; background:#999; color:#fff;
				span {display:block; font-family:Times New Roman, serif; padding:20px 10px; background:#555; font-weight:bold}
			}
		}
		div.flipped { z-index:2; @include shadows(#888, 2px, 2px, 4px);
			img {visibility:hidden}
			.backside {visibility:visible}
		}
		div.ui-draggable-dragging {z-index:3; @include shadows(#555, 8px, 8px, 16px)}

		/* extras */
		.logo-mom {position:absolute; bottom:90px; left:20px; z-index:1}
		.ear {position:absolute; top:30%; background-color:#866d59; @include corners(0, 5px, 5px, 0); @include shadows(#333, 2px, 2px, 4px); color:#fff; text-align:center; width:130px; height:160px; text-decoration:none; font-size:13px}
			.ear img {display:block; margin:15px 20px; margin-top:30px; margin-left:35px}
			.ear:hover {background-color:#6e4e3e}
		.ear.left {left:0}
		.ear.right {right:0; background-color:#3b5997; @include corners(5px, 0, 0, 5px);}
			.ear.right:hover {background-color:#2b416f}
		#trash {position:absolute; bottom:0; left:0; width:100%; height:75px; z-index:1000; cursor:pointer; 
			.text {position:absolute; z-index:2; top:0; left:0; margin-top:20px; color:#fff; font-size:90%; width:100%; height:50px; font-weight:bold}
				span.full {display:none; color:#ddd; font-size:90%; font-weight:normal}
			.background {background:#856d59; @include opacity(50); position:absolute; top:0; left:0; width:100%; height:75px; z-index:1}
		}
			#trash.full {@include opacity(70);
				span.full {display:inline-block}
			}
			#trash.hover {
				.background {background:#fff; @include opacity(100)}
				span {color:#000}
				span.full {display:none}
			}
	}
}
.ico {width:32px; height:32px; vertical-align:middle}
#toolbar textarea {font-family:"Monotype Corsiva", "Apple Chancery", cursive; }
